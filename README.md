# automatic_password_generator
This project is an automatic password generator developed using Python. It generates strong random passwords every 3 seconds without any user input. The program runs continuously and uses the current system time to ensure each password is unique. It combines letters, numbers, and symbols, helping understand automation, randomness..
